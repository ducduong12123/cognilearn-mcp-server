[project]
name = "cognilearn-mcp-server"
version = "0.1.0"
description = "MCP server that gives AI tutors long-term memory about each learner (CogniLearn)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-generativeai>=0.8.5",
    "gunicorn>=23.0.0",
    "mcp[cli,server]>=1.13.1",
    "python-dotenv>=1.1.1",
    "supabase>=2.18.1",
    "uvicorn>=0.35.0",
]
